import React from 'react';
import classes from './LoaderUI.module.css';


const LoaderUI = () => {
    return (
        <div className={classes.loader}>

        </div>
    );
};

export default LoaderUI;